//Write a function that takes an array and a number and returns the sum of first element and the number.

const sumFirstElement = (arr, i) => arr[0] + i;

console.log(sumFirstElement([1, 2, 3], 10)); // 11
